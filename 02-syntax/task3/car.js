const carName = "Toyota Camry";
let maxCarSpeed = 200;
let carOwner = "";

alert("Марка машины: " + carName);
alert("Максимальная скорость: " + maxCarSpeed + " км/ч");
alert("Владелец: " + carOwner.length > 0 ? carOwner : "Не указан");